# Dependencias del servicio tecnicos. Versiones FIJADAS para que la imagen sea
# reproducible.
#
# No lleva httpx ni requests: este servicio NO llama a nadie por HTTP, solo
# consume y publica eventos.
#
# Las versiones se eligieron con wheels disponibles tanto para el Python 3.12
# del contenedor como para intérpretes más nuevos, para que el servicio también
# se pueda correr suelto en la máquina de cualquiera del equipo.

fastapi==0.141.1
uvicorn[standard]==0.52.1

pydantic==2.13.4
pydantic-settings==2.15.0

sqlalchemy==2.0.51
psycopg[binary]==3.3.4

# Cliente asíncrono de RabbitMQ: convive con el bucle de eventos de FastAPI,
# así que el consumidor corre como tarea del mismo loop y no en un hilo aparte.
aio-pika==10.0.1
