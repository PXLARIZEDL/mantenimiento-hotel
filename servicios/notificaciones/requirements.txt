# Dependencias del servicio notificaciones. Es la lista más corta del proyecto a
# propósito: no tiene base de datos y no llama a nadie.
#
# No lleva sqlalchemy ni psycopg — agregar el driver sería la primera señal de
# que alguien está por romper la decisión de que este servicio no persiste nada
# (docs/adr/002-limites-contextos.md). Tampoco httpx ni requests: todo lo que
# necesita viene dentro del evento.
#
# Mismas versiones que servicios/tecnicos/requirements.txt, para que los dos
# servicios Python no diverjan sin motivo.

fastapi==0.141.1
uvicorn[standard]==0.52.1

# pydantic entra igual como dependencia de fastapi; se fija explícitamente
# porque las respuestas de la bandeja dependen de él.
pydantic==2.13.4

aio-pika==10.0.1
